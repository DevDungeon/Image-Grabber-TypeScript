export function grabImages(url: string) {
    // do HTTP GET (url)
    // cheerio parse the html contents
    // extract all img tags
        // extrac the src attribute
    // download the src url image to images/ dir
    console.log('Attempting to grab images from: ' + url)
}
